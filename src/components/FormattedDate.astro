---
import dayjs from 'dayjs';

type Props = {
  date: Date;
  withDay?: boolean;
};
const { date, withDay = false } = Astro.props;

const formattedDate = dayjs(date).format(
  withDay ? 'dddd, D MMM YYYY' : 'D MMM YYYY'
);
---

<time datetime={date.toISOString()}>{formattedDate}</time>
