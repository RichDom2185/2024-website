---
type Props = {
  date: Date;
};
const { date } = Astro.props;
---

<time datetime={date.toISOString()}
  >{
    date.toLocaleDateString('en-UK', {
      day: 'numeric',
      month: 'short',
      year: 'numeric',
    })
  }</time
>
